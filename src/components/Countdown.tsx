import { Card } from 'react-bootstrap'

const Countdown = () => {
  return (
    <Card className="h-100">
      <Card.Body className="d-flex align-items-center justify-content-center">
        <h4 className="m-0">Countdown</h4>
      </Card.Body>
    </Card>
  )
}

export default Countdown
